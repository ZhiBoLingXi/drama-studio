# 验证记录

当前源码版本为 `0.2.0`；已发布的 `v0.1.0` 技能包仍保留旧检查行为，不包含此次改动。本页区分工具验收与实际业务验收，不以离线测试代替平台接入。

## 本地验收

2026-09-21：18 项标准库测试在 Python 3.10 与 3.12 下全部通过，覆盖安装预览不写入、重复安装、独立运行、升级保留旧版本、保留用户修改与新增文件、拒绝无回执目录与符号链接、安装锁、宿主目录选择、可重复打包与导出包独立运行。

工具用例覆盖项目初始化不覆盖、缺失语料不能跳过、短语料不能通过、相同文本能检出、独立示例能通过、无效阈值、重复集标题、未识别格式与短篇检查失败。

2026-09-22：0.2.0 的 30 项测试在 Python 3.10 与 3.12 下全部通过，仓库静态检查通过。剧集检查默认仅统计；显式规格才核对数值；历史阈值仅为可选预设。回归测试覆盖未配置不宣称通过、单条规则不启用其他规则、配置来源与优先级、非法配置报错、旧参数提示、空正文、同义重复集号、无对白、格式不完整、地点标签不合并、比例比较精度和跨集状态。创作判断始终单独处理。

命令：

```sh
python3.10 -B -m unittest discover -s tests -v
python3.12 -B -m unittest discover -s tests -v
python3.10 -B scripts/validate.py
```

仓库检查覆盖 Python 语法、本地 Markdown 链接、代码围栏、技能元数据、分发文件类型及个人绝对路径。未安装第三方运行依赖。

## 真实验收状态（2026-09-23更新）

| 项目 | 当前状态 |
| --- | --- |
| Codex／Claude Code 真实技能发现 | 当前账号fresh Codex会话原生发现项目.agents技能并执行doctor；Claude Code与新用户未验证 |
| ChatGPT／WorkBuddy ZIP 导入 | 包已生成；实际账号导入未验证 |
| 新用户 mali-story OAuth 与业务读取 | 安装协议已提供；未替新用户完成登录 |
| 视频上传与收费拉片 | 已读真实模板及证据，发现摘要集数冲突；用户授权最多300积分，但线上不能保证上限，未提交新付费任务 |
| 夸克整剧自动下载 | 原任务完成78/78；本地77集齐全、全部探测、3集完整解码成功；未从搜索重放 |
| 全剧创作与编辑验收 | 本轮另产出8集悬疑、8集爱情、24集职场、6集奇幻完整初稿，独立模型审读后定向修订；真实编剧、60–100集稳定性未验证 |
| 跨宿主／跨设备接续 | 提供串行接续规则；远端功能依赖实际部署 |
| GitHub 仓库与 Release 附件 | `ZhiBoLingXi/drama-studio` 私有仓库；v0.1.0 提供两个宿主技能 ZIP，访问需读取权限 |

下一步先让一位使用 Codex 或 Claude Code 的编剧完成安装、只读模板查询、已有拉片模板到选题确认、四项送审材料与审批后续写的试用，再扩展到其他宿主和全剧交付。

结果验收另见 [六集试跑记录](../evals/screenplay-outcomes/run-2026-09-22.md)。不能用离线测试通过或这次作者自评替代真实编剧的稿件采用。

后续已执行 [自主迭代与独立比较](../evals/screenplay-outcomes/iteration-2026-09-22.md)。两位模型评审均偏好新结构，定向修补后编辑判断为可继续编辑；这不是系统稳定成功率，也不替代服装专业验证、围读或真人采用。本轮技能方法更新后，Python 3.10 的30项测试及静态检查通过。

2026-09-22 防同质化方法接入：新增生活材料到场面试写的开发流程、开发记录与两阶段阅读模板，接通策划／写作／接续／交付入口。隔离安装与两个宿主ZIP均包含新增文件，安装副本31个相对链接可解析，Python 3.10 的30项测试通过。六个合成行为用例由维护者自查，已知判读期望，不当作独立评审或实际剧本提质证据。详见 [执行记录](../evals/screenplay-outcomes/diversity-run-2026-09-22.md)。

2026-09-23：新增模板覆盖检查及6项回归，Python 3.10的36项测试通过；新检查不认证全剧完整性、原片一致或创作质量。补齐外部编辑保护、角色压缩、24集新会话接续、前瞻阅读和奇幻类型对照；原稿／修稿／真实事件日志保存在被忽略的项目目录。详见[本轮结果](../evals/screenplay-outcomes/coverage-run-2026-09-23.md)及[覆盖矩阵](../evals/screenplay-outcomes/scenario-matrix.md)。

2026-09-23可选红果榜单接入：新增按需MCP／文件入口说明与候选交接模板，无新增运行依赖、采集器或远程服务。三个新Codex上下文完成合成资料的缺日、摘要降级、已确认片段研究测试，维护者按实际报告核对；36项测试、技能结构检查、22文件临时安装校验和双宿主ZIP相对链接检查通过。没有在ChatGPT网页实际导入、调用活动红果MCP或新增收费拉片。详见[榜单行为验收](../evals/market-research/README.md)。

2026-09-23 两次审批流程修正：选题确认后一次交付剧情框架、人物小传、完整集纲与前10集正文，整包获批后再续写剩余正文。四个fresh Codex CLI用例实际验证了仅回复集数、选题已确认、待审接续、整包获批接续；36项标准库测试及23文件技能分发检查通过。未替用户升级桌面安装，不视为真人剧本质量验收。详见[审批行为验收](../evals/approval-flow/README.md)。
